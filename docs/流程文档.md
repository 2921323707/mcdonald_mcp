# 麦当劳 MCP 平台 — 架构与流程文档

![架构](../static/structure.png)

### OpenClaw 交互时序图

```mermaid
sequenceDiagram
    participant U as 👤 用户
    participant AI as 🤖 OpenClaw AI
    participant MCP as MCP Server<br/>(mcd-mcp)
    participant MCD as 麦当劳后端

    U->>AI: "我想点个麦辣鸡腿堡套餐"
    AI->>AI: 理解意图 → 需要先查地址
    AI->>MCP: delivery-query-addresses()
    MCP->>MCD: 查询用户配送地址
    MCD-->>MCP: 返回地址列表 + beCode/storeCode
    MCP-->>AI: 返回结果
    AI->>AI: 提取 beCode + storeCode
    AI->>MCP: query-meals(beCode, storeCode)
    MCP-->>AI: 返回餐品列表
    AI->>AI: 匹配用户选择的餐品
    AI->>MCP: query-store-coupons(beCode, storeCode)
    MCP-->>AI: 返回可用优惠券
    AI->>MCP: calculate-price(beCode, storeCode, items)
    MCP-->>AI: 返回价格明细
    AI->>AI: 展示给用户确认
    U->>AI: "确认下单"
    AI->>MCP: create-order(addressId, beCode, storeCode, items)
    MCP-->>AI: 返回订单号
    AI-->>U: 订单创建成功，订单号：xxx
    AI->>MCP: query-order(orderId)
    MCP-->>AI: 配送状态更新
    AI-->>U: 骑手正在配送中...
```

---

## OpenClaw 命令行调用

### 完整点餐流程 curl 示例demo

```bash
# Step 1: 查询配送地址（获取 beCode + storeCode）
curl http://127.0.0.1:5000/api/tools/call \
  -H "Content-Type: application/json" \
  -d '{"name": "delivery-query-addresses", "arguments": {}}'

# Step 2: 用返回的 beCode + storeCode 查询门店餐品
curl http://127.0.0.1:5000/api/tools/call \
  -H "Content-Type: application/json" \
  -d '{"name": "query-meals", "arguments": {"storeCode": "S001", "beCode": "BE001"}}'

# Step 3: 查询门店可用优惠券
curl http://127.0.0.1:5000/api/tools/call \
  -H "Content-Type: application/json" \
  -d '{"name": "query-store-coupons", "arguments": {"storeCode": "S001", "beCode": "BE001"}}'

# Step 4: 计算价格（items 从 Step2 获取 productCode）
curl http://127.0.0.1:5000/api/tools/call \
  -H "Content-Type: application/json" \
  -d '{"name": "calculate-price", "arguments": {
    "storeCode": "S001",
    "beCode": "BE001",
    "items": [{"productCode": "10001", "quantity": 1}]
  }}'

# Step 5: 创建订单（addressId 从 Step1 获取）
curl http://127.0.0.1:5000/api/tools/call \
  -H "Content-Type: application/json" \
  -d '{"name": "create-order", "arguments": {
    "addressId": "12345",
    "storeCode": "S001",
    "beCode": "BE001",
    "items": [{"productCode": "10001", "quantity": 1}]
  }}'

# Step 6: 查询订单状态
curl http://127.0.0.1:5000/api/tools/call \
  -H "Content-Type: application/json" \
  -d '{"name": "query-order", "arguments": {"orderId": "1030938730000733964700499858"}}'
```

---

## 其他功能模块

### - 优惠券流程

```mermaid
flowchart LR
    A["available-coupons<br/>查询可领券"] --> B["auto-bind-coupons<br/>一键自动领券"]
    B --> C["query-my-coupons<br/>查看卡包"]
```

### - 积分兑换流程

```mermaid
flowchart LR
    A["query-my-account<br/>查积分余额"] --> B["mall-points-products<br/>查可兑换商品"]
    B --> C["mall-product-detail<br/>商品详情"]
    C --> D["mall-create-order<br/>积分下单兑换"]
```

### - 下单流程

```mermaid
flowchart LR
    A["delivery-query-addresses<br/>查询配送地址"] --> B["query-meals<br/>查询餐品列表"]
    B --> C["query-store-coupons<br/>查询门店可用优惠券"]
    C --> D["calculate-price<br/>计算价格"]
    D --> E["create-order<br/>创建订单"]
    E --> F["query-order<br/>查询订单状态"]
```

